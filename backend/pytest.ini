[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
addopts = -s -m "not integration and not slow"
asyncio_mode = auto
markers =
    integration: marks tests as integration tests that need external API keys
    slow: marks tests as slow or Gemini-dependent; skipped without GEMINI_API_KEY
