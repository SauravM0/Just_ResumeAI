\documentclass[letterpaper,11pt]{article}

% ATS safety: T1/UTF-8 keep ligatures and Unicode text searchable in PDF parsers.
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage[usenames,dvipsnames]{color}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage[english]{babel}

% ATS safety: no header/footer package regions, which can confuse content order.
\pagestyle{empty}

% Slightly tighter geometry gives the budget service room to keep stronger bullets.
\addtolength{\oddsidemargin}{-0.55in}
\addtolength{\evensidemargin}{-0.55in}
\addtolength{\textwidth}{1.1in}
\addtolength{\topmargin}{-0.55in}
\addtolength{\textheight}{1.1in}

\urlstyle{same}

\raggedbottom
\raggedright
\setlength{\tabcolsep}{0in}

% ATS safety: bold text preserves normal section text better than small caps.
\titleformat{\section}{
  \vspace{-4pt}\raggedright\large\bfseries
}{}{0em}{}[\color{black}\titlerule \vspace{-5pt}]

\pdfgentounicode=1

\newcommand{\resumeItem}[1]{
  \item\small{
    {#1 \vspace{-3pt}}
  }
}

% tabular* displays the 4th argument on the right without adding a second column layout.
\newcommand{\resumeSubheading}[4]{
  \vspace{-2pt}\item
    \begin{tabular*}{0.97\textwidth}[t]{l@{\extracolsep{\fill}}r}
      \textbf{#1} & {\small #2}\\
      \textit{\small#3} & \textit{\small #4}\\
    \end{tabular*}\vspace{-7pt}
}

\newcommand{\resumeProjectHeading}[2]{
    \item
    \small\textbf{#1}\vspace{-7pt}
}

\renewcommand\labelitemii{$\vcenter{\hbox{\tiny$\bullet$}}$}

\newcommand{\resumeSubHeadingListStart}{\begin{itemize}[leftmargin=0.15in, label={}]}
\newcommand{\resumeSubHeadingListEnd}{\end{itemize}}
\newcommand{\resumeItemListStart}{\begin{itemize}[leftmargin=0.2in]}
\newcommand{\resumeItemListEnd}{\end{itemize}\vspace{-5pt}}

\begin{document}

\begin{center}
    {\Huge \textbf{<< contact_name >>}} \\ \vspace{3pt}
    <% if target_title %>{\small \textit{<< target_title >>}} \\ \vspace{3pt}<% endif %>
    \small
    <% if contact_email %>\href{mailto:<< contact_email >>}{\underline{<< contact_email >>}}<% endif %>
    <% if contact_phone %> <% if contact_email %>$|$<% endif %> << contact_phone >><% endif %>
    <% if contact_location %> <% if contact_email or contact_phone %>$|$<% endif %> << contact_location >><% endif %>
    <% if contact_linkedin %> <% if contact_email or contact_phone or contact_location %>$|$<% endif %> \href{<< contact_linkedin >>}{\underline{LinkedIn}}<% endif %>
    <% if contact_github %> <% if contact_email or contact_phone or contact_location or contact_linkedin %>$|$<% endif %> \href{<< contact_github >>}{\underline{GitHub}}<% endif %>
    <% if contact_portfolio %> <% if contact_email or contact_phone or contact_location or contact_linkedin or contact_github %>$|$<% endif %> \href{<< contact_portfolio >>}{\underline{Portfolio}}<% endif %>
\end{center}

<% for section_name in section_order_list %>
<% if section_name == "summary" and summary %>
\section{Professional Summary}
\small{<< summary >>}
\vspace{-4pt}

<% elif section_name == "education" and education %>
\section{Education}
  \resumeSubHeadingListStart
    <% for edu in education %>
    \resumeSubheading
      {<< edu.institution >>}{<< edu.date_range >>}
      {<< edu.degree_line >>}{<< edu.detail_line >>}
      <% if edu.coursework %>
      \resumeItemListStart
        \resumeItem{Relevant Coursework: << ", ".join(edu.coursework) >>}
      \resumeItemListEnd
      <% endif %>
    <% endfor %>
  \resumeSubHeadingListEnd

<% elif section_name in ("technical skills", "skills") and skills %>
\section{Technical Skills}
 \begin{itemize}[leftmargin=0.15in, label={}]
    \small{\item{
     <% for skill_group in skills %>
     \textbf{<< skill_group.category >>}{: << skill_group.skills >>}<% if not loop.last %> \\<% endif %>
     <% endfor %>
    }}
 \end{itemize}

<% elif section_name == "experience" and experiences %>
\section{Professional Experience}
  \resumeSubHeadingListStart
    <% for exp in experiences %>
    \resumeSubheading
      {<< exp.title >>}{<< exp.date_range >>}
      {<< exp.company_line >>}{<< exp.location or "" >>}
      <% if exp.bullets %>
      \resumeItemListStart
        <% for bullet in exp.bullets %>
        \resumeItem{<< bullet >>}
        <% endfor %>
      \resumeItemListEnd
      <% endif %>
    <% endfor %>
  \resumeSubHeadingListEnd

<% elif section_name == "projects" and projects %>
\section{Projects}
    \resumeSubHeadingListStart
      <% for project in projects %>
      \resumeProjectHeading
          {<< project.name >><% if project.technologies %> $|$ \emph{<< project.technologies >>}<% endif %>}{}
          <% if project.bullets %>
          \resumeItemListStart
            <% for bullet in project.bullets %>
            \resumeItem{<< bullet >>}
            <% endfor %>
          \resumeItemListEnd
          <% endif %>
      <% endfor %>
    \resumeSubHeadingListEnd

<% elif section_name in ("achievements", "awards") and achievements %>
\section{Achievements \& Awards}
  \resumeSubHeadingListStart
    <% for item in achievements %>
    \resumeSubheading
      {<< item.title >>}{<< item.date or "" >>}
      {<< item.issuer or "" >>}{}
      <% if item.description %>
      \resumeItemListStart
        \resumeItem{<< item.description >>}
      \resumeItemListEnd
      <% endif %>
    <% endfor %>
  \resumeSubHeadingListEnd

<% elif section_name == "certifications" and certifications %>
\section{Certifications}
  \resumeSubHeadingListStart
    <% for cert in certifications %>
    \resumeSubheading
      {<< cert.name >>}{<< cert.date or "" >>}
      {<< cert.issuer or "" >>}{}
    <% endfor %>
  \resumeSubHeadingListEnd
<% endif %>
<% endfor %>

<% for custom_section in custom_sections %>
\section{<< custom_section.title >>}
  \resumeItemListStart
    <% for item in custom_section.items %>
    \resumeItem{<< item >>}
    <% endfor %>
  \resumeItemListEnd
<% endfor %>

\end{document}
